import express from "express";
import multer from "multer";
import fs from "node:fs";
import path from "node:path";
import crypto from "node:crypto";
import { fileURLToPath } from "node:url";
import { chromium } from "playwright";
import { execFile } from "node:child_process";
import OpenAI from "openai";
const __filename=fileURLToPath(import.meta.url),__dirname=path.dirname(__filename),app=express(),port=Number(process.env.PORT||3000),uploadDir="/tmp/html-video-uploads";
let activeRender=false;fs.mkdirSync(uploadDir,{recursive:true});
const upload=multer({dest:uploadDir,limits:{fileSize:10*1024*1024},fileFilter:(_,f,cb)=>{const ok=f.mimetype==='text/html'||/\.html?$/i.test(f.originalname);cb(ok?null:new Error('HTML files only'),ok)}});
app.use(express.json({limit:'1mb'}));
const num=(v,d,min,max)=>{const n=Number(v);return Number.isFinite(n)?Math.min(Math.max(n,min),max):d};
const clean=(w,f)=>{try{if(w)fs.rmSync(w,{recursive:true,force:true})}catch{}try{if(f)fs.unlinkSync(f)}catch{}};
const ffmpeg=args=>new Promise((res,rej)=>execFile('ffmpeg',args,{maxBuffer:12*1024*1024},(e,o,s)=>e?rej(Error(s||e.message)):res(o)));
const renderSize=(w,h)=>{const max=921600,p=w*h;if(p<=max)return{width:w,height:h};const s=Math.sqrt(max/p);return{width:Math.max(320,Math.floor(w*s)),height:Math.max(320,Math.floor(h*s))}};
const browserArgs=['--no-sandbox','--disable-setuid-sandbox','--disable-dev-shm-usage','--use-gl=swiftshader','--enable-webgl','--ignore-gpu-blocklist','--disable-gpu-sandbox','--disable-background-timer-throttling','--disable-renderer-backgrounding','--disable-backgrounding-occluded-windows'];
app.get('/api/health',(_,r)=>r.json({ok:true,openai:Boolean(process.env.OPENAI_API_KEY),provider:'openai',model:process.env.OPENAI_MODEL||'gpt-5.6-luna',renderer:true,rendererEngine:'chromium-ffmpeg'}));
app.post('/api/openai',async(req,res)=>{try{if(!process.env.OPENAI_API_KEY)throw Error('OPENAI_API_KEY غير مضبوط على الخادم.');const {messages,system=''}=req.body||{};const input=(Array.isArray(messages)?messages:[]).filter(m=>m&&(m.role==='user'||m.role==='assistant')&&typeof m.content==='string').map(m=>({role:m.role,content:m.content}));if(!input.length)return res.status(400).json({error:'messages مطلوب.'});const ai=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const out=await ai.responses.create({model:process.env.OPENAI_MODEL||'gpt-5.6-luna',instructions:system,input,max_output_tokens:Number(process.env.OPENAI_MAX_OUTPUT_TOKENS||8000)});res.json({text:out.output_text||'',model:out.model,responseId:out.id})}catch(e){console.error(e);res.status(Number(e.status)>=400?Number(e.status):500).json({error:e.message||'OpenAI error'})}});
app.post('/render',upload.single('html'),async(req,res)=>{let browser=null,context=null,work=null;try{if(activeRender)return res.status(429).json({error:'محرك الرندر مشغول حالياً.'});if(!req.file)return res.status(400).json({error:'يرجى رفع ملف HTML.'});activeRender=true;const duration=num(req.body.duration,5,3,30),width=Math.round(num(req.body.width,1280,320,1920)),height=Math.round(num(req.body.height,720,320,1920)),fps=Math.round(num(req.body.fps,30,15,30)),internal=renderSize(width,height);work=path.join('/tmp',`html-video-${Date.now()}-${crypto.randomBytes(4).toString('hex')}`);const frames=path.join(work,'frames'),htmlPath=path.join(work,'index.html'),out=path.join(work,'html-render.mp4');fs.mkdirSync(frames,{recursive:true});fs.copyFileSync(req.file.path,htmlPath);browser=await chromium.launch({headless:true,args:browserArgs});context=await browser.newContext({viewport:internal,deviceScaleFactor:1,ignoreHTTPSErrors:true});const page=await context.newPage();page.setDefaultTimeout(120000);await page.goto(`file://${htmlPath}`,{waitUntil:'networkidle',timeout:120000});await page.waitForFunction(()=>document.readyState==='complete',{timeout:30000}).catch(()=>{});if(await page.locator('canvas').count()){await page.waitForFunction(()=>window.HTML_VIDEO_READY===true||[...document.querySelectorAll('canvas')].some(c=>c.width>0&&c.height>0),{timeout:30000}).catch(()=>{})}await page.evaluate(()=>new Promise(requestAnimationFrame));const total=Math.ceil(duration*fps);for(let i=0;i<total;i++){await page.screenshot({path:path.join(frames,`frame-${String(i).padStart(6,'0')}.jpg`),type:'jpeg',quality:90,animations:'allow'});if(i+1<total)await page.waitForTimeout(1000/fps)}await context.close();context=null;await browser.close();browser=null;const scale=internal.width!==width||internal.height!==height?['-vf',`scale=${width}:${height}:flags=lanczos`]:[];await ffmpeg(['-y','-framerate',String(fps),'-i',path.join(frames,'frame-%06d.jpg'),'-c:v','libx264','-preset','veryfast','-crf','21',...scale,'-pix_fmt','yuv420p','-movflags','+faststart',out]);activeRender=false;res.download(out,'html-render.mp4',()=>clean(work,req.file.path))}catch(e){activeRender=false;try{await context?.close()}catch{}try{await browser?.close()}catch{}clean(work,req.file?.path);console.error('Render error',e);res.status(500).json({error:e.message||'فشل إنشاء الفيديو.'})}});
app.use((e,_q,r,_n)=>r.status(e.code==='LIMIT_FILE_SIZE'?413:400).json({error:e.message||'حدث خطأ'}));
app.use(express.static(path.join(__dirname,'dist')));app.get(/.*/,(req,res)=>res.sendFile(path.join(__dirname,'dist','index.html')));app.listen(port,()=>console.log(`HTML → VIDEO integrated server on ${port}`));
